CREATE TABLE scores
(id VARCHAR(20) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
 item_id INTEGER,
 score_type_id INTEGER,
 score INTEGER);
--;;
ALTER TABLE scores
    ADD CONSTRAINT "SC_TYPE_ID" FOREIGN KEY (score_type)
    REFERENCES score_type (id);
--;;
ALTER TABLE scores
    ADD CONSTRAINT "SC_ITEM_ID" FOREIGN_KEY (item )
    REFERENCES item  (id)
